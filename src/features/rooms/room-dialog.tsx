"use client";
import { useActionState, useState } from "react";
import { LoaderCircle, Pencil, Plus } from "lucide-react";
import { saveRoomBed } from "./actions";
import type { ActionState } from "@/types/hospital";
import { Button } from "@/components/ui/button";import { Checkbox } from "@/components/ui/checkbox";import { Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { useAutoCloseDialog } from "@/hooks/use-auto-close-dialog";
type Room={id:string;roomNumber:string;bedNumber:string;floor:string;roomType:string|null;active:boolean};
export function RoomDialog({room}:{room?:Room}){const[state,action,pending]=useActionState(saveRoomBed,{ok:false} as ActionState);const{open,setOpen}=useAutoCloseDialog(state,"Room/bed saved.");const[active,setActive]=useState(room?.active??true);return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger render={<Button size={room?"sm":"default"} variant={room?"ghost":"default"}/>}>{room?<Pencil/>:<Plus/>}{room?"Edit":"Add room / bed"}</DialogTrigger><DialogContent><form action={action} className="contents"><DialogHeader><DialogTitle>{room?"Edit":"Add"} room / bed</DialogTitle><DialogDescription>Add one row per assignable bed. Multi-bed rooms can share the same room number.</DialogDescription></DialogHeader><input type="hidden" name="id" value={room?.id??""}/><input type="hidden" name="active" value={active?"on":""}/>{state.message&&!state.ok?<p className="rounded-md bg-destructive/10 p-3 text-sm text-destructive">{state.message}</p>:null}<div className="grid gap-4 sm:grid-cols-2">{[["roomNumber","Room number",room?.roomNumber??""],["bedNumber","Bed number",room?.bedNumber??"1"],["floor","Floor",room?.floor??""],["roomType","Room type",room?.roomType??""]].map(([name,label,value])=><div className="space-y-2" key={name}><Label htmlFor={`room-${name}`}>{label}</Label><Input id={`room-${name}`} name={name} defaultValue={value} required={name!=="roomType"}/><p className="text-xs text-destructive">{state.fieldErrors?.[name]?.[0]}</p></div>)}<label className="flex items-center gap-2 text-sm"><Checkbox checked={active} onCheckedChange={(v)=>setActive(v===true)}/> Available for assignment</label></div><DialogFooter showCloseButton><Button disabled={pending} type="submit">{pending?<LoaderCircle className="animate-spin"/>:<Plus/>} Save</Button></DialogFooter></form></DialogContent></Dialog>}
