import "server-only";
import { notFound } from "next/navigation";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export type IpPrintData={id:string;ticket_number:string;admission_at:string;discharge_at:string|null;room:string|null;bed:string|null;admission_reason:string|null;status:string;final_diagnosis:string|null;hospital_course:string|null;treatment_summary:string|null;discharge_medicines:string|null;discharge_advice:string|null;follow_up:string|null;patients:{name:string;phone_normalized:string;dob:string|null;gender:string}|null;doctors:{display_name:string;qualification:string|null;registration_number:string|null}|null;ip_charges:Array<{id:string;created_at:string;category:string;item:string;quantity:number;rate_paise:number;amount_paise:number}>;ip_payments:Array<{id:string;created_at:string;amount_paise:number;mode:string;reference:string|null}>};
export async function getIpPrintData(id:string){const supabase=await createSupabaseServerClient();const{data,error}=await supabase.from("ip_tickets").select("id,ticket_number,admission_at,discharge_at,room,bed,admission_reason,status,final_diagnosis,hospital_course,treatment_summary,discharge_medicines,discharge_advice,follow_up,patients(name,phone_normalized,dob,gender),doctors(display_name,qualification,registration_number),ip_charges(id,created_at,category,item,quantity,rate_paise,amount_paise),ip_payments(id,created_at,amount_paise,mode,reference)").eq("id",id).single();if(error||!data)notFound();return data as unknown as IpPrintData}
