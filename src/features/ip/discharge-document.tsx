import { calculateAge,formatHospitalDate } from "@/lib/domain/date";
import type { IpPrintData } from "./print-data";
export function DischargeDocument({ticket}:{ticket:IpPrintData}){const sections=[["Admission Reason",ticket.admission_reason],["Final Diagnosis",ticket.final_diagnosis],["Hospital Course",ticket.hospital_course],["Treatment",ticket.treatment_summary],["Discharge Medicines",ticket.discharge_medicines],["Advice",ticket.discharge_advice],["Follow-up",ticket.follow_up]].filter(([,value])=>value);return <article className="min-h-[270mm] border border-black/20 p-7"><header className="flex justify-between border-b-2 border-primary pb-4"><div><h1 className="text-xl font-bold text-primary">Meenakshi Hospital</h1><p>Discharge Summary</p></div><div className="text-right"><p className="font-bold">{ticket.ticket_number}</p><p>{ticket.doctors?.display_name}</p><p>{ticket.doctors?.qualification}</p><p>Registration: {ticket.doctors?.registration_number??"—"}</p></div></header><section className="my-4 grid grid-cols-2 gap-2 rounded border p-3 text-sm sm:grid-cols-4"><p><b>Patient:</b> {ticket.patients?.name}</p><p><b>Patient ID:</b> {ticket.patients?.phone_normalized}</p><p><b>Age/Gender:</b> {ticket.patients?.dob?calculateAge(ticket.patients.dob):"—"} / {ticket.patients?.gender}</p><p><b>Room/Bed:</b> {ticket.room??"—"}/{ticket.bed??"—"}</p><p><b>Admission:</b> {formatHospitalDate(ticket.admission_at,true)}</p><p><b>Discharge:</b> {ticket.discharge_at?formatHospitalDate(ticket.discharge_at,true):"Pending"}</p></section><div className="space-y-5">{sections.map(([title,value])=><section className="break-inside-avoid" key={title}><h2 className="border-b pb-1 text-sm font-bold uppercase tracking-wide text-primary">{title}</h2><p className="mt-2 whitespace-pre-wrap text-sm leading-relaxed">{value}</p></section>)}</div><footer className="mt-16 flex justify-between border-t pt-4 text-xs"><p>Digital discharge summary issued by Meenakshi Hospital</p><div className="text-right"><p className="font-bold">{ticket.doctors?.display_name}</p><p>{ticket.doctors?.registration_number}</p></div></footer></article>}
