# Printing

Token output is compact and must never include fee, collected amount, balance, or payment mode. Prescriptions, IP bills, and discharge summaries use A4 portrait. Print routes render semantic document HTML and the global print stylesheet hides the application shell. Test Chrome desktop/mobile triggers, one- and two-page prescriptions, repeating table headers, and page breaks before release.
